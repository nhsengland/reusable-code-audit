# Data pipeline snippets

## Included snippets

- `create_holdout_split.py` — reusable train/holdout + LM split generator.
- `setup_input_json.py` — pipeline setup utility for generating model input JSON from raw files.

These snippets can seed a shared NHS data engineering/pipeline helper package.
