# Multimodal snippets

## Included snippets

- `feature_name_mapping.py` — modality feature map helper for static/time-series inputs.
- `embedding_dataset_wrappers.py` — text/image dataset wrappers for multimodal embedding pipelines.

These are reusable foundations for multimodal NHS projects using image/text/time-series data.
