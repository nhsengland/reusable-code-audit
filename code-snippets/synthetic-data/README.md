# Synthetic Data snippets

## Included snippets

- `distribution_metrics.py` — tabular synthetic data quality and privacy metric wrappers.
- `experiment_io_helpers.py` — reusable experiment/file path helpers.

These are strong candidates for a shared `synthetic_data` utility module.
