# NLP / Text processing snippets

## Included snippets

- `prepare_notes_for_lm.py` — configurable text cleaning and note export pipeline.
- `clean_text_pipeline.R` — reusable R text normalization + tokenization functions.

These patterns repeat across clinical text projects and are suitable for a shared NLP preprocessing package.
